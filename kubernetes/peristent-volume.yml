apiVersion: v1
kind: PersistentVolume
metadata:
  name: crud-pv
  namespace: crud
spec:
  capacity:
    storage: 1Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /db/data
